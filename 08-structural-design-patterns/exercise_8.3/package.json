{
  "name": "exercise_8.3",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf node_modules coverage",
    "test:watch": "./node_modules/.bin/mocha --parallel --watch",
    "test": "./node_modules/.bin/mocha --parallel",
    "coverage-report": "./node_modules/.bin/c8 --clean -r html npm test",
    "coverage-report:check": "./node_modules/.bin/c8 --clean check-coverage --lines 100 --per-file npm test",
    "coverage-report:watch": "./node_modules/.bin/nodemon -x \"npm run coverage-report\"",
    "coverage-report:serve": "./node_modules/.bin/live-server --open=coverage"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "c8": "^7.8.0",
    "chai": "^4.3.4",
    "live-server": "^1.2.1",
    "mocha": "^9.0.3",
    "nodemon": "^2.0.12",
    "sinon": "^11.1.2"
  },
  "dependencies": {
    "ansi-styles": "^6.1.0"
  }
}
